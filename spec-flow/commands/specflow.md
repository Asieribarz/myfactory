---
description: Flujo spec → plan → convergencia → ejecución para frontend, backend, arquitectura o fullstack
argument-hint: <frontend|backend|arquitectura|fullstack|continuar> <descripción o slug> [--auto]
---

Aplica la skill `spec-flow` con estos argumentos: $ARGUMENTS

- Primer argumento: tipo de proyecto, o `continuar` seguido del slug para reanudar desde `status.md`.
- Resto: descripción de lo que hay que construir.
- Si aparece `--auto`, usa modo auto (solo para en bloqueos y acciones destructivas); si no, modo supervisado.
